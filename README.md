# Roommate Expense Splitter
Software Engineering II Project

A web application that allows roommates to track shared expenses, split bills, and manage payments within a household.

## Frontend Setup

 Make sure the backend server is running before starting the frontend, otherwise the app will not work.

```bash
cd FrontEnd
npm install
npm run dev
